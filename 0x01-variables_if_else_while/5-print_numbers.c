#include  <stdio.h>

/**
 * main - Entry point
 *
 * Description: Numbers;
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num = 0;

	for (num <= 9; num++)
	{
		printf("%d", num);
	}
	printf('\n');

	return (0);
}
