#include <stdio.h>

/**
 * main - Entry point
 * Description:  I sometimes suffer from insomnia;
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}

	return (0);
}

