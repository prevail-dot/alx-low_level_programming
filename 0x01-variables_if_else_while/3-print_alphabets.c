#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: alphABET;
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int x;

	for (x = 'a'; x <= 'z', 'Z'; a++)
	{
		putchar(x);
	}
	{
		putchar(toupper(x));
	}
	{
		putchar('\n');
	}

	return (0);
}
